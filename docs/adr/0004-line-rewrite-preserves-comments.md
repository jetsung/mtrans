# 配置写回用行文本改写而非 TOML 序列化

`config.toml` 由 TUI 向导写回时按行替换键值、保留原有注释与顺序，不使用 toml crate 反序列化后再序列化——用户手写的中文注释与手工排版是有价值的配置资产，序列化会全部丢失。行改写基建的边界：仅替换 `[setting]`/`[ci]` 段内既有键或在其表头前插入新键，`[registries]` 条目以文本块整体追加；手工解析需容忍布尔/数值/字符串三种 TOML 值形态。
