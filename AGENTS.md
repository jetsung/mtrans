# mtrans

把容器镜像复制到**目标注册表**（也可反向拉回本地）。复制一律触发远程 GitHub Actions 工作流托管复制（ADR-0001）；凭据不落配置文件、`sync` 时用加密口令 `[setting].auth_passphrase` 把 `~/.docker/config.json` 的 `auth` 加密成 `target_auth_secret` 传入工作流，流水线内用 Secret `AUTH_PASSPHRASE` 解密并打掩码（ADR-0002）；支持 Windows/Linux/macOS 三端。

## CLI 帮助与回显规范（硬性要求）

1. **中英双语**：帮助信息（顶层 `--help`、各子命令 `--help`、`help`/`man`）与各命令、子命令的运行时回显，均须支持中文与英文，按系统语言（`LANG`/`LC_ALL`/`LC_MESSAGES`，`zh*` 判中文）切换，**默认英文**。
   - 落点：子命令与参数文案在 `src/cli.rs`（clap 属性，中文为默认源）；英文覆盖在 `src/i18n.rs` 的 `localize()`，与中文逐条同构（每个子命令、每条参数、每组分组都必须双语齐备，新增子命令时两侧同步添加）。
   - 运行时输出（进度/成功/错误提示）统一走 `src/i18n.rs` 的中英成对文案函数，禁止在业务代码里硬编码单语字符串。
2. **结构清晰**：帮助与回显**该换行就换行、该缩进就缩进**，必须让用户更直观、清楚地明白功能与使用情况：
   - 顶层帮助按职责分组（镜像复制 / 凭据 / 配置 / 工作流模板 / 帮助），组间空行、命令列与说明列对齐；
   - 子命令 `--help` 用「一行摘要 + 空行 + 缩进字段表（字段名对齐）」结构，禁止单行超长句；
   - 运行时回显按逻辑块分行，多行参数块缩进展示、键列对齐，块间空行分隔；
   - 提示文案（尤其 TUI 的 prompt）不得嵌入长值（如完整口令），避免终端折行导致重绘残留；长值在操作确认后单独一行回显。

## 文档索引

- 术语：`docs/CONTEXT.md`
- 配置 schema 与行为契约：`docs/SPEC.md`
- 架构决策：`docs/adr/`
- 仓库门户与快速开始：`README.md`
